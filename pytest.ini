[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    network: tests that require live internet access (auto-skipped when offline)
    integration: end-to-end tests hitting the FastAPI app
filterwarnings =
    ignore::DeprecationWarning
